import React from 'react';

const Movie = ( {movie }) => {
  return (
    <div>
    <div>hi</div>
    <img src={movie.image} alt="stuffandthings" ></img>
    </div>
  )
}

export default Movie;